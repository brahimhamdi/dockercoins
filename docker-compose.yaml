services:
  rng:
    build: ./rng
  hasher:
    build: ./hasher
  webui:
    build: ./webui
    ports:
    - 8000:80
    volumes:
    - "./webui/files/:/files/"
  redis:
    image: redis
  worker:
    build: ./worker
